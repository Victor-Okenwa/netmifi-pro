import { AppLoaderOverlay } from "@/components/shared/app-loader";

/**
 * Next.js route loading UI — brand spinner on a dimmed overlay.
 */
export default function Loading() {
	return <AppLoaderOverlay />;
}
